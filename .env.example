# Database (SQLite file path, used at runtime by the app)
DATABASE_URL="file:./data/portfolio.db"

# JWT secret for admin authentication — CHANGE THIS in production!
JWT_SECRET="change-me-to-a-long-random-secret"

# Node environment
NODE_ENV="production"
